# =====================
# Core
# =====================
numpy==1.26.4
pandas==2.3.3
scipy==1.13.1
pydantic==2.11.9
python-dotenv==1.1.1
PyYAML==6.0.2
requests==2.32.3
typing_extensions==4.12.2

# =====================
# PyTorch + CUDA 12.1 stack (as currently installed)
# =====================
# If CUDA version differs, adjust the +cu121 suffix accordingly.
torch==2.5.1+cu121
torchvision==0.20.1+cu121
torchaudio==2.5.1

# =====================
# Computer Vision / Training
# =====================
opencv-python==4.11.0.86
albumentations==2.0.8
pillow==11.0.0
timm==1.0.20
einops==0.8.1
matplotlib==3.9.4
seaborn==0.13.2

# =====================
# YOLO (for sparse crowds)
# =====================
ultralytics==8.3.204
ultralytics-thop==2.0.17

# =====================
# API (FastAPI inference server)
# =====================
fastapi==0.117.1
uvicorn==0.37.0
starlette==0.48.0
python-multipart==0.0.20

# =====================
# Logging & Monitoring (optional but useful)
# =====================
wandb==0.22.2
tensorboard==2.20.0

# =====================
# Utilities
# =====================
psutil==6.1.0
rich==13.9.4
tqdm==4.67.1

# =====================
# Pinned versions derived from current working environment.
# Removed: dev tools (black, flake8, mypy, pytest), Jupyter stack, Sphinx docs,
# OS-specific wheels, conda build paths, and indirect deps that pip will resolve.
# After install, run:
#   pip check
#   python -c "import torch, cv2, ultralytics; print('OK')"
# =====================
