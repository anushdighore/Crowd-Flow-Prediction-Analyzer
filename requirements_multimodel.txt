# requirements_multimodel.txt
# Multi-Model Crowd Counting System - Python Dependencies

# Core Dependencies
torch>=2.0.0
torchvision>=0.15.0
numpy<2  # PyTorch compatibility

# Web Framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
websockets>=12.0
pydantic>=2.0.0

# Image Processing
opencv-python>=4.8.0
Pillow>=10.0.0

# Model-Specific Dependencies
# ---------------------------

# For YOLOv8 (Object Detection)
ultralytics>=8.0.0

# For VMamba (Optional - if using official implementation)
# timm>=0.9.0
# einops>=0.7.0

# Utilities
tqdm>=4.65.0

# Development (Optional)
# pytest>=7.4.0
# black>=23.0.0
# flake8>=6.0.0

# Notes:
# ------
# 1. Install with: pip install -r requirements_multimodel.txt
# 2. Some models work with randomly initialized weights if checkpoints unavailable
# 3. YOLOv8 will auto-download weights on first use
# 4. GPU support requires CUDA-enabled PyTorch build
